fn main(){

    let symptoms = vec!["Headache","Stomach Pain","High Temperature","Vomit","Cough", "Blood in poo" ];

    // println!("The current number is {}",numbers[0]);
    // println!("The current number is {}",numbers[1]);
    // println!("The current number is {}",numbers[2]);
    // println!("The current number is {}",numbers[3]);

    for symptom in symptoms {
        if symptom == "High Temperature" {
            let kelly = "Kelly";
        }
        println!("{}", kelly);
    }

}